<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
<mapper namespace="com.web.haechi.mapper.NewsDAO">
    <select id="selectAll" resultType="com.web.haechi.vo.NewsVO">
        SELECT 
        	*
        FROM 
        	NEWS
    </select>
</mapper>